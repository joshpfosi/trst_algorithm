/* Implements a function velocity() which takes as arguments wind speed, angle,
 * and heading and returns the velocity predicted by the polar diagram
 */

double velocity(double speed, double angle, double heading) {
    (void)speed; (void)angle; (void)heading;
}
